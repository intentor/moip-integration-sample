<!DOCTYPE html>
<html>
  <head>
  </head>
  <body>
    <div>
      <img src="img/intentor_logo.png" />
      <p>We sell digital goodies to make you happy!</p>
      <ul>
          <% products.forEach(function(r) { %>
              <li><%= r.id %> - <%= r.name %></li>
          <% }); %>
      </ul>
    </div>
  </body>
</html>
